<!-- Parent: ../AGENTS.md -->
# src

Python source root. Always set `PYTHONPATH=src` or install with `pip install -e .`.

- Do not add modules directly here — place them inside `lazyverilogpy/`
- See `lazyverilogpy/AGENTS.md` for the package details
