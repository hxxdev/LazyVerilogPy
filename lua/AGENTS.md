<!-- Parent: ../AGENTS.md -->
# lua

Neovim plugin Lua layer. Neovim resolves `require("lazyverilogpy.foo")` → `lua/lazyverilogpy/foo.lua`.

- Add files inside `lazyverilogpy/`, not directly here
- See `lazyverilogpy/AGENTS.md` for module details
- Test: open a `.sv` file in Neovim, confirm attach with `:LspInfo`
