FIX
@demo/memory.sv content is change module parameter #(WIDTH = 4) is gone when saving file. Find out the cause. I have checked its not the cause of format_source() with @tmp.sv
FIX
ActiveParamter is not highlighted when I write sum(.i_a @demo/memory_top.sv. When I write the consequent '(', signature help window closes instead of anything being highlighted.
