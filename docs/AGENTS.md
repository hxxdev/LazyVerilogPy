<!-- Parent: ../AGENTS.md -->
# docs

Reference documentation for LazyVerilogPy users and contributors.

## Files

| File | Purpose |
|------|---------|
| `format-options.md` | All `[format]` TOML options with type, default, and examples |
| `lint.md` | `[codebase]` TOML section — multi-file `.f` filelist loading |
| `hover.md` | Hover feature — what `K` shows for ports, variables, instances |
| `autoarg.md` | AutoArg — fills module port-list header from body `input`/`output`/`inout` declarations |
| `autofunc.md` | AutoFunc — generates function/task call-sites; covers named vs positional args and TOML config |
| `rtltree.md` | RtlTree — interactive module hierarchy viewer (forward and reverse instantiation) |
